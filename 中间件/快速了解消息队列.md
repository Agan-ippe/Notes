# 什么是消息队列

是一种用于连接多个系统，帮助多个系统紧密协作的技术。

举个例子 A 给 B 发了一条消息，B很忙，不知道什么时候能看到A发的消息。

A不想一直等B，又不想消息丢失。

就需要一个中转站来存放这些消息，A只需要将消息发出就行了，B有空的时候再取出消息。

![image-20251123190009087](https://raw.githubusercontent.com/Agan-ippe/typora_pic/main/imgs/image-20251123190009087.png)

这个中转站就是消息中间件。

# 消息队列的作用

- 流量削峰
- 应用解耦
- 异步处理

## 流量削峰

流量削峰的本质就是限流，在高并发场景下，例如秒杀、抢购等活动，大量的用户请求同时涌入，可能导致后端服务无法承受压力而崩溃。消息队列可以作为一个缓冲区，将请求放入队列，后端服务按照自身处理能力逐步处理请求，从而保护后端服务。



## 应用解耦

以电商系统为例，应用中包含订单系统、库存系统、物流系统、支付系统、积分系统等等。用户下单后，订单系统需要通知积分系统。一般的做法是：订单系统直接调用积分系统的接口。这就使得应用系统间的耦合特别紧密。如果积分系统无法访问，则积分处理失败，从而导致订单失败。

加入消息队列之后，用户下单后，订单系统完成下单业务后，将消息写入消息队列，返回用户订单下单成功。积分系统通过订阅下单消息的方式获取下单通知消息，从而进行积分操作。实现订单系统与库存系统的应用解耦。如果，在下单时积分系统系统异常，也不影响用户正常下单，因为下单后，订单系统写入消息队列就不再关心其他的后续操作。



## 异步处理

异步处理，就是将一些非核心的业务流程以异步并行的方式执行，从而减少请求响应时间，提高系统吞吐量。

以下单为例，用户下单后需要生成订单、赠送活动积分、赠送红包、发送下单成功通知等一系列业务处理。假设三个业务节点每个使用100毫秒钟，不考虑网络等其他开销，则串行方式的时间是400毫秒，并行的时间只需要200毫秒。这样就大大提高了系统的吞吐量。



# 主流技术

- ActiveMQ：官方社区维护少，基本上不用
- RabbitMQ
- Kafka：专为大数据而生
- RocketMQ：专为金融互联网领域而生
- ZeroMQ
- Pulsar：专为云原生而生
- Apache InLong（Tube）



# 技术对比

吞吐量：IO、并发

时效性：类似延迟、消息的发送、到达时间

| 技术名称 | 吞吐量           | 时效性                 | 可用性             | 消息可靠性                       | 优势                                                 | 功能支持                                       | 应用场景                                                     |
| -------- | ---------------- | ---------------------- | ------------------ | -------------------------------- | ---------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------ |
| ActiveMQ | 万级             | 毫秒级                 | 高，主从架构       | 较低概率丢失数据                 | 易学简单                                             | 非常完备                                       | 中小型企业、项目                                             |
| RabbitMQ | 万级             | 毫秒级（可低至微秒级） | 高，主从架构       | 基本不丢                         | 生态良好（支持多种语言）                             | 非常完备                                       | 适用于绝大部分分布式项目，初学者入门推荐先学RabbitMQ         |
| Kafka    | 百万级（或更高） | 毫秒级                 | 非常高，分布式架构 | 经过参数配置，可做到最小概率不丢 | 吞吐量大、可靠性、可用性高，拥有强大的数据流处理能力 | 功能较为简单，对标大数据领域                   | 适用于大数据场景，比如构建日志收集系统，实时数据流传输、事件流收集传输 |
| RockeMQ  | 十万至百万级     | 毫秒级                 | 非常高，分布式架构 | 经过参数配置，可做到0丢失        | 吞吐量大，可扩展性高                                 | 非常完备                                       | 适用于金融、电商等对可靠性要求较高的场景，也适合大规模的消   |
| Pulsar   | 百万级           | 毫秒级                 | 非常高，分布式架构 | 经过参数配置，可做到0丢失        | 新一代消息队列，技术架构先进，提供多种发布订阅模式   | 云原生新一代消息中间件、支持多租户、跨域部署等 | 适用于云原生（大规模、高并发的分布式系统），适合实时分析、事件流处理、IoT数据处理等。 |

